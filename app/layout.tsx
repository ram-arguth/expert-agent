import type { Metadata } from 'next';
import { GeistSans } from 'geist/font/sans';
import { GeistMono } from 'geist/font/mono';
import './globals.css';
import { Providers } from './providers';

export const metadata: Metadata = {
  title: 'Expert AI - Domain Expert AI Agents',
  description:
    'Access specialized AI expert agents for any domain. Get high-quality, context-aware answers from AI agents preloaded with domain expertise.',
  keywords: ['AI', 'expert agents', 'domain expertise', 'AI assistant', 'intelligent agents'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
